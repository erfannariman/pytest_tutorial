name: main
on: [push]
jobs:
   run:
      runs-on: [ubuntu-latest]
   steps:
      - uses: actions/checkout@v2
      - name: test
      run:
         pip install -r requirements.txt
         python main.py